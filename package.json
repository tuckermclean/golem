{
  "name": "golem-engine",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "games/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "test:some-hero-legacy": "npm ci --prefix games/some-hero/legacy && npm test --prefix games/some-hero/legacy && npm run test:e2e --prefix games/some-hero/legacy"
  }
}
