{
  "name": "golem-engine",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "games/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "test:some-hero-legacy": "npm ci --prefix games/some-hero/legacy && npm test --prefix games/some-hero/legacy && npm run test:e2e --prefix games/some-hero/legacy",
    "test:ceremony": "node --test games/some-hero/ceremony/*.ceremony.test.js",
    "freeze:verify": "npm run verify:golem --workspace @golem-engine/testkit && npm run test:ceremony && npm run verify:tdp --workspace @golem-engine/testkit && npm run verify:tdp-solutions --workspace @golem-engine/testkit && npm run verify:some-hero-content --workspace @golem-engine/some-hero && npm run test:ceremony-kernel --workspace @golem-engine/some-hero",
    "//freeze:verify": "some-hero content (DELTA S1): structural compile + content-review (all strings === live legacy) + hash-stability golden + no-legacy-import, as a permanent behavior-freeze gate"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "typescript": "6.0.3"
  }
}
